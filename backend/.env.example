# ===========================================================================
# Turismo que se Sente — Backend environment variables
# Copy this file to ".env" and fill in real values. NEVER commit the real .env.
# ===========================================================================

# --- Postgres connection (required) ---
# Connection string for the Postgres instance (e.g. Neon: postgresql://user:pass@host/db?sslmode=require).
DATABASE_URL=postgresql://localhost:5432/turismo_que_se_sente

# --- LLM integration (optional) ---
# Emergent LLM key used for automatic translations (OpenAI gpt-4o-mini).
# Leave empty to disable live translation.
EMERGENT_LLM_KEY=

# --- Security / Auth (required) ---
# Secret used to sign admin JWT tokens. Use a long, random, unique value.
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=

# --- Default admin account (required) ---
# Seeded on first startup if no admin exists. Change the password after first login.
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=change-me-strong-password

# --- Vercel Blob (required for /admin/upload-image) ---
# Image uploads are stored on Vercel Blob (the deployed function's filesystem
# is read-only). Auto-populated when a Blob store is connected to the Vercel
# project; set manually for local dev with a token from the Vercel dashboard.
BLOB_READ_WRITE_TOKEN=
# Optional: folder where uploads are organized in the Blob store.
BLOB_UPLOAD_FOLDER=turismo-que-se-sente/uploads
